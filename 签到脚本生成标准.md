# 签到脚本生成标准（har2sign 输出规范 v2）

> 目的：本文是转换工具（`har2sign.py` / `QD转Python.py`）生成脚本的**唯一格式标准**。
> 任何 AI / 开发者读完本文后，应当能在**不运行工具**的情况下，手写或生成出与它**格式、行为完全一致**的脚本。
>
> 输入：QD / 青龙任务模板 `.har`（含 `comment` / `request` / `rule`），**不是**浏览器整页录制的 HAR。
> 产物：Python 3、尽可能标准库、少第三方依赖，`python xxx.py` 直接可跑（必须依赖时须在脚本头部写明安装说明）。

---

## 1. 命名、位置与导入

| 项 | 标准 |
|---|---|
| 目录 | `ZWZW/`（Arcadia 只扫描此目录） |
| 文件名 | `<站点>_账号.py`（账号模式） / `<站点>_Cookie.py`（Cookie 模式）；旧后缀 `_sign.py` 已废弃 |
| 改名同步 | 改脚本名必须同步 `arcadia/tasks.json` 的键 + 脚本头部第一条 cron 注释（用 `tools/生成定时头.py` 维护），否则被判"未配置" |
| 自动登记 | 生成后写入导入列表（tasks.json：键=文件名、值=cron）；已存在则覆盖 cron；文件缺失/解析失败则跳过、不新建 |
| 时间窗 | 自动分配 cron 默认落在 **7–21 点**（每 5 分钟一档、跳过已占槽；排满退化为全天）；`--slot-range` / `QD_SLOT_RANGE` 可调（支持跨天 `22-6`） |
| 编码 | 文件 UTF-8；`ENTRIES` 用 `pprint.pformat(entries, width=200, sort_dicts=False)`（**不可** `json.dumps`，否则 `true/false/null` 非合法 Python 字面量，含 `"login": true` 会 `NameError`） |

---

## 2. 输入 `.har` 结构

```jsonc
[{
  "comment": "登录",
  "request": {
    "method": "POST",
    "url": "https://site.com/wp-admin/admin-ajax.php",
    "headers": [{"name": "Cookie", "value": "{{cookie}}"}],
    "data": "user={{username|urlencode}}&pwd={{password|urlencode}}"
  },
  "rule": {
    "success_asserts": [{"re": "登录成功", "from": "content"}],
    "failed_asserts":  [{"re": "用户名或密码不正确", "from": "content"}],
    "extract_variables": [{"name": "nonce", "re": "\"ajax_nonce\":\"(.*?)\"", "from": "content"}]
  }
}]
```

- `from`：`content`（默认，匹配响应体）或 `status`（匹配状态码字符串）。
- `url` 可为 `api://util/...` —— QD 内部工具，**不发请求**。
- 正则里的 `\uXXXX` 字面中文，生成前**必须解码为真实字符**，否则断言永远匹配不上。
- ⚠️ **用 QD / 签到插件录制"仅登录 + 签到"两步的精简任务 HAR**（每步带 `success_asserts`/`failed_asserts` + 汇总 `api://util/unicode`）。浏览器整页 HAR 含海量静态/第三方请求且无业务断言，无法判定结果。

---

## 3. 输出骨架（固定顺序，不可调换）

```
#!/usr/bin/env python3
# 3 行头部注释（生成说明 / 运行方式 / 多账户写法）
import os, re, sys, ssl, json, gzip, zlib, html, time, random, subprocess, urllib.request, urllib.parse, urllib.error
from http.cookiejar import CookieJar

# ===== 内置配置（环境变量优先；按模式只列需要的项）=====
___CONFIG___
# =================================

BASE = ___BASE___
ENTRIES = ___ENTRIES___          # pprint.pformat(entries, width=200, sort_dicts=False)
UA = (...)

# —— 固定函数（见 §4，由工具逐字生成、不得改动）——
def build_opener / _apply_filter / _eval_val / _render_expr / render /
    _split_multi / _split_pairs / _is_placeholder / _ensure_dep / _clean_domain /
    _sleep_between / _cookie_cache_path / _load_cache / _save_cache / run_one / run

if __name__ == "__main__":
    sys.exit(run())
```

- `___ACCOUNTS_BLOCK___` 内联在 `run()` 中，**顶层语句必须 4 空格**（嵌套 8 空格），写成 8 空格会 `IndentationError`。
- 账号模式 `run_one` 返回 4 元组 `(ok, 输出文本, cookie串, 登录变量)`；输出取**最后一个非空 log**（最终汇总步），不是多行拼接。`build_opener` 返回 `(opener, jar)`。

---

## 4. 生成规则要点

### 4.1 `BASE` / `ENTRIES`
- `BASE`：取首个 `http(s)` url 的协议+域名，只作标识不参请求。
- `ENTRIES`：`pprint.pformat(entries, width=200, sort_dicts=False)`，**不裁剪字段**（空 `cookies`/`mimeType` 也保留）。

### 4.2 模式识别
| 模式 | 判定 |
|---|---|
| Cookie 模式 | 任一 header 名为 `cookie`，或值含 `{{cookie}}` |
| 账号模式 | 非 Cookie 模式（默认） |
| 自定义域名 | 任一 url 小写后含 `{{domain}}` |

> Cookie 模式若 header 是**真实字面值**（不含 `{{`），改写为 `{{cookie}}`，原值作 `BUILTIN_COOKIE` 默认。

### 4.3 配置块变量（按模式只列需要的项，每行中文注释；环境变量统一 `<SITE_TAG>_XXX` 前缀，封装在 `_env()`）

| 变量 | 出现条件 | 默认 | 说明 |
|---|---|---|---|
| `BUILTIN_ACCOUNT` | 账号模式（必） | `""` | 账号#密码，多组换行/@@/&&；`<SITE>_ACCOUNT` |
| `BUILTIN_COOKIE` | Cookie 模式 | `""`/字面 | 会话 Cookie，多账户换行/@@/&&；`<SITE>_COOKIE` |
| `BUILTIN_USER` / `BUILTIN_PASS` | 账号模式（旧） | `""` | 旧写法，ACCOUNT 空时生效（单密码自动复用） |
| `BUILTIN_DOMAIN` | 自定义域名 | `""` | 站点域名（不带协议）；`<SITE>_DOMAIN` |
| `BUILTIN_PROXY` | 总是 | `""` | `<SITE>_PROXY` |
| `BUILTIN_DELAY` | 总是 | `"5-10"` | 间隔：固定(3)/区间(2-5)/0；`<SITE>_DELAY` |
| `BUILTIN_FORCE_LOGIN` | 账号模式 | `False` | 每次账号密码登录、不缓存；`<SITE>_FORCE_LOGIN` |
| `BUILTIN_TLS_IMPERSONATE` | 可选 | `""` | 浏览器 TLS 拟真(`chrome` 等)，留空=urllib；`<SITE>_TLS_IMPERSONATE` |
| `BUILTIN_AUTO_INSTALL` | 总是 | `True` | 依赖缺失自动 pip；`<SITE>_AUTO_INSTALL=0` 关 |
| `BUILTIN_STRICT_EMPTY` | 总是 | `True` | 汇总变量全空判失败；`<SITE>_STRICT_EMPTY=0` 关 |

### 4.4 账号密码单变量规范（账号模式默认）
- `<SITE>_ACCOUNT='账号#密码'`；`#` 与 `:` 均可，**取最先出现切一次**（另一符号可进密码）；多组换行/`@@`/`&&` 等价；只填账号则密码空。
- 优先级：`ACCOUNT`（环境变量→内置）**优先**，解析空才回退 `USER`+`PASS`（旧写法保留"单密码复用"）。

### 4.5 账户块 `___ACCOUNTS_BLOCK___`
定义 `accounts = [{ctx, jar, label}]`：
- Cookie 模式 `jar=False`，label = Cookie 前 10 字符脱敏；
- 账号模式 `jar=True`，label = 用户名（兼容 `{{user/username/password/pwd}}` 等写法）；
- 自定义域名注入 `c["DOMAIN"]`。
- 无账户即 `return 1` 并打印配置提示。

### 4.6 其它
- **`\uXXXX` 解码**：生成前对 rule 的 `re` 字段解码为真中文。
- **登录步骤标记**：账号模式扫描 entry，同时含密码类+账号类模板变量则置 `"login": true`；仅账号模式标记，Cookie 模式不标记不缓存。
- **Cookie 缓存（仅账号模式）**：登录后把会话 cookie + 登录提取变量写 `<名>.cookie_cache.json`（脚本同目录）；下次复用、跳过登录；失效（命中未登录）自动清缓存重登。青龙持久化、Arcadia 临时容器缺失则退化每次登录，两平台都安全。Cookie 模式完全不生成缓存代码。
- **`FORCE_LOGIN`**：不靠 Cookie 维持登录的站点（纯 token / 每次需重登）设 `True`，每次完整走登录、不读写缓存。

---

## 5. 固定代码（工具生成，不得改动）

§3 列出的函数由转换工具模板**逐字生成粘贴**，行为须与本文 §6 一致；手写脚本亦须实现同等逻辑。关键不变量（任一脚本都须满足）：

- 剔除 `Accept-Encoding` 与 `:` 伪头；有 `ctx["cookie"]` 且步骤未声明 Cookie 头则自动补 `Cookie`；
- 自动跳过静态扩展名与跨域请求（**仅 `url.startswith("http")` 时生效**；`api://` 不参与，否则汇总步被误判跨域→无输出）；
- gzip/deflate 解压、`\uXXXX` 解码；断言顺序固定；占位/未登录/空数据三层兜底；
- Cookie 缓存读写与失效重登；启用 TLS 拟真时 `run()` 惰性 `from curl_cffi import ...`、建 `Session(impersonate=...)`、经 `_send()` 分发（默认空走 urllib）。

---

## 6. 运行时语义（行为标准）

### 6.1 变量渲染与过滤器
- `{{expr}}` 支持 `{{a|filter}}` 与 `{{unicode(...)}}`；缺失变量渲染为空。
- 过滤器：`urlencode`(`quote(safe="")`) / `unicode`(原样) / `default(x)`(空返回 x) / 其它原样。
- `display=True`（仅 `api://` 步）时 `urlencode` **不生效**，保留原始文本便于展示。

### 6.2 请求构造
- 跳过 `:` 开头伪头；**必须剔除 `Accept-Encoding`**；Cookie 补齐（见 §5）；
- POST body 仅 `data` 非空时传，UTF-8 编码；
- 自动跳过静态/跨域噪音（仅 http 请求）；超时 60s，单请求超时不崩脚本。

### 6.3 响应处理
按 `Content-Encoding` 解压 → `utf-8` → 尝试 `json.loads/dumps(ensure_ascii=False)` 解码 `\uXXXX`；`HTTPError`(4xx/5xx) 也读响应体走断言。

### 6.4 断言与失败处理（顺序不可变）
1. 先 `failed_asserts` → 命中即账户失败（返回原因 + 响应前 500 字）；
2. 再 `success_asserts`（**或逻辑**，任一条命中即成功）；
3. 非空且全不命中 → 失败；为空 → 不校验；
4. 单账户失败**不中断**后续账户。

### 6.5 变量提取 / `api://` 工具
- `extract_variables` 顺序 `re.search`，有分组取 `group(1)` 否则 `group(0)` 写 `ctx`。
- `util/unicode`：取 `content`/`s`，`html_unescape=true` 先转义，解 `\uXXXX`，换行归一；`util/string/replace`：`t→p` 普通/正则替换。结果 append 到 `logs`，由 `run()` 取最后非空统一输出。

### 6.6 多账户与延迟
- `_split_multi`：列表 / 换行 / `@@` / `&&`；单个 `&` 仅每段以 `key=` 开头才切分（保护含 `&` 的 Cookie）。
- 账号模式推荐 `_split_pairs`（账号#密码，不做 `&` 切分）；空回退 `USER`/`PASS`。
- `_sleep_between(i)`：首个不等待；`<SITE>_DELAY` 支持固定 / 区间 / 0。

### 6.7 输出格式与退出码（固定文案，必对齐）
```
共 N 个账户待执行
======== [i/N] <label> ========
<输出 或 失败原因>
==========================
执行完毕：共 N 个账户，成功 X，失败 Y
```
全成功 `exit 0`；有失败 `exit 1`；无输出打印 `(无输出)`。

### 6.8 Cookie 缓存与登录跳过
仅账号模式参与；`<SITE>_COOKIE_CACHE` 可改路径；`FORCE_LOGIN` 时不缓存（详见 §4.6）。

### 6.9 成功判定与防误判（三层兜底，按执行顺序）
1. **占位检测**：`_is_placeholder`（空 / `xxx` 类占位词 / 单字符重复且长度≤6）→ 打印提示并 `return 1`，**不发请求**。
2. **未登录兜底**（每 HTTP 步，在 failed 之后）：`AUTH_ERROR_RE`（密码错误/账号或密码错误/登录失败/用户不存在）**任意步**命中即失败；`NOT_LOGGED_IN_RE`（请先登录/未登录/尚未登录/需要登录/登录后再/凭证失效）**仅 `auth_ready` 后**查（`auth_ready`=登录步完成 / 复用缓存 / Cookie 模式），避免登录前置页误伤。刻意不含单独的"登录"二字，以免误伤"登录成功"等正常文案。
3. **空数据检测**：汇总步 `api://util/unicode` 引用变量全空 → 失败（受 `STRICT_EMPTY` 控制，默认开）。

---

## 7. 环境变量总表

| 变量 | 作用 | 多账户 |
|---|---|---|
| `<SITE>_ACCOUNT` | 账号#密码（推荐） | 支持 |
| `<SITE>_USER` / `<SITE>_PASS` | 旧写法，按序配对 | 支持 |
| `<SITE>_COOKIE` | 会话 Cookie | 支持 |
| `<SITE>_DOMAIN` | 自定义域名 | 支持（按序） |
| `<SITE>_PROXY` | 代理 | 否 |
| `<SITE>_DELAY` | 账户间隔 | — |
| `<SITE>_COOKIE_CACHE` | Cookie 缓存路径 | 否 |
| `<SITE>_FORCE_LOGIN` | 强制每次登录、不缓存 | 否 |
| `<SITE>_TLS_IMPERSONATE` | 浏览器 TLS 拟真（空=urllib） | 否 |
| `<SITE>_AUTO_INSTALL` | 依赖缺失自动安装 | 否 |
| `<SITE>_STRICT_EMPTY` | 汇总变量全空判失败 | 否 |

优先级：**环境变量 > `BUILTIN_*`**。

---

## 8. 生成自检清单（核心）

- [ ] 仅含该模式所需 `BUILTIN_*`，每项中文注释
- [ ] 账户块顶层 4 空格；定义 `accounts`(ctx/jar/label)
- [ ] 账号模式 `BUILTIN_ACCOUNT` + `_split_pairs`，空回退 USER/PASS
- [ ] 防误判三件套齐备（占位 / 未登录分组 / 空数据）
- [ ] `\uXXXX` 已解码；未引入第三方库；`html` 非 `_html`
- [ ] 去 `Accept-Encoding` 与 `:` 伪头；有 Cookie 补齐
- [ ] 含 Cookie 缓存函数与 `run()` 读写/失效重登（Cookie 模式除外）
- [ ] 启用 TLS 时惰性 `curl_cffi` + `_send()` 分发（默认 urllib）
- [ ] 输出文案/退出码符合 §6.7；`py_compile` 通过

---

## 9. 已知坑（务必避免，精简）

1. 账户块顶层 8 空格 → `IndentationError`。
2. 不补齐 Cookie → 后续"请先登录"、变量全空。
3. `&` 分隔须每段 `key=` 开头，否则误切 Cookie 值。
4. 只能用 `html.unescape`（来自 `import html`），勿用 `_html`。
5. 中文断言须先解码 `\uXXXX`。
6. 必须剔除 `Accept-Encoding`，否则可能拿到 br 压缩无法解析。
7. `ENTRIES` 用 `pprint` 不用 `json.dumps`（防 `NameError: true`）。
8. `api://` 步 `failed_asserts` 须对渲染后 `out` 匹配，否则失效不被识别、误判成功。
9. 输出只取最后非空 log，勿 `join` 多行（避免"登录成功"与结果重复）。
10. 用 QD 精简任务 HAR，勿整页录制。
11. `TLS_IMPERSONATE` 默认空，仅实测被 TLS 风控才开（开错会强制依赖 curl_cffi）。
12. 跨域/静态过滤须 `url.startswith("http")`，否则 `api://` 汇总步被跳过 → `(无输出)`。
13. 跨域判定须子域放行（`_cross_domain`），否则 `api.` 子域接口变量提取为空（解析不全）。
14. `BASE` 含 `{{DOMAIN}}` 时须在 `run_one` 内先 `render(BASE, ctx)` 再比域，否则全步跳过。
15. 第三方授权域名用 `--allow-cross` / `ALLOW_CROSS` 放行（属正常业务）。
16. 未登录关键词分组、仅 `auth_ready` 后查，否则登录前置页（登录页/取 token 页）误伤。

---

## 10. 特殊场景可用技术（通用指引，非默认）

> 以下技术均为 **opt-in / 默认关闭**，保证不启用时脚本仍是零依赖标准库实现。遇到对应站点按需启用即可，**不必为每个站点单列特例章节**。

- **Cookie 模式（最轻）**：绕过登录页反爬 / reCAPTCHA，直接复用浏览器复制的会话 Cookie（如 `remember_web_...`）。适用：登录页带验证码 / 加密 CSRF、纯 HTTP 无法自动登录的站点。仅填 Cookie，不读写本地缓存。
- **浏览器级 TLS 指纹拟真（`curl_cffi`）**：`BUILTIN_TLS_IMPERSONATE="chrome"` 启用，`Session(impersonate=...)` 还原握手绕过 JA3 / TLS 风控；默认 urllib 零依赖。适用：标准库被 `RemoteDisconnected` / TLS 指纹拦截的站点。
- **浏览器自动化（Playwright）**：用于 SPA + reCAPTCHA，或需从前端请求头拦截令牌（如 `Bearer`）的站点。代价最重，仅在纯 HTTP 确实无法突破时采用。
- **WebSocket 长连接**：用于需实时状态 / 推送（如开机、进度上报）的面板协议。
- **滑块 / 验证码复现（纯算法）**：部分主题滑块是"前端算答案、服务端仅比对"的幌子，无需图像识别 / ddddocr，本地复现算法即可。
- **多步时序 / 纠偏**：长流程（如看视频进度按服务端时间纠偏）用循环上报 + 重试实现。
- **原则**：优先最轻方案（Cookie 模式 > TLS 拟真 > 浏览器）；若特殊逻辑超出通用模板表达能力（滑块算法、每次全新登录约束等），作为**手写特例脚本**维护，但尽量把可复用能力沉淀为 opt-in 特性。
